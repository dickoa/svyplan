% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{print.svyplan}
\alias{print.svyplan}
\alias{print.svyplan_n}
\alias{print.svyplan_cluster}
\alias{print.svyplan_prec}
\alias{print.svyplan_varcomp}
\alias{print.svyplan_power}
\alias{format.svyplan_n}
\alias{format.svyplan_cluster}
\alias{format.svyplan_prec}
\alias{format.svyplan_varcomp}
\alias{format.svyplan_power}
\alias{confint.svyplan_n}
\alias{confint.svyplan_prec}
\alias{as.integer.svyplan_n}
\alias{as.double.svyplan_n}
\alias{as.integer.svyplan_cluster}
\alias{as.double.svyplan_cluster}
\alias{as.data.frame.svyplan_n}
\alias{as.data.frame.svyplan_prec}
\alias{as.data.frame.svyplan_varcomp}
\alias{as.data.frame.svyplan_cluster}
\alias{as.integer.svyplan_power}
\alias{as.double.svyplan_power}
\alias{as.data.frame.svyplan_power}
\alias{print.svyplan_strata}
\alias{format.svyplan_strata}
\alias{as.data.frame.svyplan_strata}
\alias{as.integer.svyplan_strata}
\alias{as.double.svyplan_strata}
\title{Print svyplan objects}
\usage{
\method{print}{svyplan_n}(x, ...)

\method{print}{svyplan_cluster}(x, ...)

\method{print}{svyplan_prec}(x, ...)

\method{print}{svyplan_varcomp}(x, ...)

\method{print}{svyplan_power}(x, ...)

\method{format}{svyplan_n}(x, ...)

\method{format}{svyplan_cluster}(x, ...)

\method{format}{svyplan_prec}(x, ...)

\method{format}{svyplan_varcomp}(x, ...)

\method{format}{svyplan_power}(x, ...)

\method{confint}{svyplan_n}(object, parm, level = 0.95, ...)

\method{confint}{svyplan_prec}(object, parm, level = 0.95, ...)

\method{as.integer}{svyplan_n}(x, ...)

\method{as.double}{svyplan_n}(x, ...)

\method{as.integer}{svyplan_cluster}(x, ...)

\method{as.double}{svyplan_cluster}(x, ...)

\method{as.data.frame}{svyplan_n}(
  x,
  row.names = NULL,
  optional = FALSE,
  stringsAsFactors = FALSE,
  validRN = TRUE,
  ...
)

\method{as.data.frame}{svyplan_prec}(
  x,
  row.names = NULL,
  optional = FALSE,
  stringsAsFactors = FALSE,
  validRN = TRUE,
  ...
)

\method{as.data.frame}{svyplan_varcomp}(
  x,
  row.names = NULL,
  optional = FALSE,
  stringsAsFactors = FALSE,
  validRN = TRUE,
  ...
)

\method{as.data.frame}{svyplan_cluster}(
  x,
  row.names = NULL,
  optional = FALSE,
  stringsAsFactors = FALSE,
  validRN = TRUE,
  ...
)

\method{as.integer}{svyplan_power}(x, ...)

\method{as.double}{svyplan_power}(x, ...)

\method{as.data.frame}{svyplan_power}(
  x,
  row.names = NULL,
  optional = FALSE,
  stringsAsFactors = FALSE,
  validRN = TRUE,
  ...
)

\method{print}{svyplan_strata}(x, ...)

\method{format}{svyplan_strata}(x, ...)

\method{as.data.frame}{svyplan_strata}(
  x,
  row.names = NULL,
  optional = FALSE,
  stringsAsFactors = FALSE,
  validRN = TRUE,
  ...
)

\method{as.integer}{svyplan_strata}(x, ...)

\method{as.double}{svyplan_strata}(x, ...)
}
\arguments{
\item{x}{A svyplan object.}

\item{...}{Additional arguments are not supported and produce an error.}

\item{object}{A svyplan object (for \code{confint} methods).}

\item{parm}{Ignored (included for S3 consistency with \code{\link[=confint]{confint()}}).}

\item{level}{Confidence level (default 0.95).}

\item{row.names, optional}{Standard \code{\link[=as.data.frame]{as.data.frame()}} arguments.}

\item{stringsAsFactors}{Logical. Retained for compatibility when a result
is converted through \code{\link[=data.frame]{data.frame()}}.}

\item{validRN}{Logical. Accepted for compatibility with \code{\link[=data.frame]{data.frame()}} in
R 4.7.0 and later. Svyplan results already have valid row names.}
}
\value{
\code{x} (or \code{object}), invisibly. \code{confint} returns a 2-column
matrix with the lower and upper confidence limits.
}
\description{
Print svyplan objects
}
\details{
\subsection{confint}{

\code{confint()} computes a confidence interval for the estimated parameter
(proportion or mean). For proportions, the interval type matches the
\code{method} used to compute the sample size (\code{"wald"}, \code{"wilson"}, or
\code{"logodds"}). For means, a symmetric z-interval is used (requires
\code{mu} in the original call).
}

\subsection{Print and coercion}{

Constrained designs (\code{n_cluster()}, \code{n_alloc()}, multistage
\code{n_multi()}) carry two representations: the continuous mathematical
optimum in the top-level fields (\code{n}, \code{cv}, \code{cost}, ...) and the
whole-unit field design in \verb{$operational}, whose cost and precision
are recomputed from the integer design. \code{print()} leads with the
field design and shows the continuous optimum as a diagnostic.

\code{as.integer(x)} returns the operational design in the same shape as
\code{x$n}: the named integer stage vector for \code{svyplan_cluster} objects,
the operational total for allocation results, and the ceiled scalar
otherwise. \code{as.double(x)} returns the continuous counterpart of the
same shape. For \code{svyplan_strata}, both coercions return the total sample
size. Boundary cutpoints remain available in \verb{$boundaries}.

\code{as.data.frame()} returns the tabular form of a result, intended as
the stable handoff to downstream packages (e.g. samplyr). For
\code{svyplan_n}: the stratum allocation table (\verb{$detail}) for
\code{n_alloc()} results, the per-domain table (\verb{$domains}, falling back
to \verb{$detail}) for \code{n_multi()} results, and a one-row summary
(\code{n}, \code{n_int}, \code{se}, \code{moe}, \code{cv}) otherwise. For \code{svyplan_cluster}:
the per-domain table when domains are present, otherwise a stage
table with columns \code{stage}, \code{n}, and \code{n_int}, where \code{n} is the continuous
optimum and \code{n_int} is the constraint-preserving operational design.
For \code{svyplan_prec}, detail-bearing multi-indicator and allocation results
return \verb{$detail}. Single-indicator results return their sample size and
precision measures in one row. For \code{svyplan_varcomp}, stratified results
return \verb{$strata}, while unstratified results return one row containing the
two- or three-stage components. For \code{svyplan_power}, one row contains the
two group sizes, their integer counterparts, power, effect, type, and the
quantity that was solved for.
}
}
\examples{
# confint on a proportion sample size
res <- n_prop(p = 0.3, moe = 0.05)
confint(res)

# confint at 90\% level
confint(res, level = 0.90)

# confint on a mean (requires mu)
res_mean <- n_mean(var = 100, mu = 50, moe = 2)
confint(res_mean)

# confint on a precision result
prec <- prec_prop(p = 0.3, n = 400)
confint(prec)

}
