% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prec_mean.R
\name{prec_mean}
\alias{prec_mean}
\alias{prec_mean.default}
\alias{prec_mean.svyplan_n}
\title{Sampling Precision for a Mean}
\usage{
prec_mean(var, ...)

\method{prec_mean}{default}(
  var,
  n,
  mu = NULL,
  alpha = 0.05,
  N = Inf,
  deff = 1,
  resp_rate = 1,
  ...
)

\method{prec_mean}{svyplan_n}(var, ...)
}
\arguments{
\item{var}{For the default method: population variance \eqn{S^2}.
For \code{svyplan_n} objects: a sample size result from \code{\link[=n_mean]{n_mean()}}.}

\item{...}{Additional arguments passed to methods.}

\item{n}{Sample size.}

\item{mu}{Population mean magnitude (positive). Required for the CV component.}

\item{alpha}{Significance level, default 0.05.}

\item{N}{Population size. \code{Inf} (default) means no finite population
correction.}

\item{deff}{Design effect multiplier (> 0). Values < 1 are valid for
efficient designs (e.g., stratified sampling with Neyman allocation).}

\item{resp_rate}{Expected response rate, in (0, 1]. Default 1 (no
adjustment). The effective sample size is deflated by \code{resp_rate}.}
}
\value{
A \code{svyplan_prec} object with components \verb{$se}, \verb{$moe}, and \verb{$cv}.
\verb{$cv} is \code{NA} when \code{mu} is not provided.
}
\description{
Compute the sampling error (SE, margin of error, CV) for estimating a
population mean given a sample size. This is the inverse of \code{\link[=n_mean]{n_mean()}}.
}
\details{
Computes the standard error for the given sample size and design
parameters, then derives the margin of error and coefficient of
variation. The effective sample size is \code{n * resp_rate / deff}, with
optional finite population correction.
}
\examples{
# Precision with n = 400
prec_mean(var = 100, n = 400, mu = 50)

# Without mu (CV will be NA)
prec_mean(var = 100, n = 400)

}
\seealso{
\code{\link[=n_mean]{n_mean()}} for the inverse (compute n from a precision target),
\code{\link[=prec_prop]{prec_prop()}} for proportions.
}
