% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svyplan.R
\name{svyplan}
\alias{svyplan}
\alias{update.svyplan}
\title{Survey Plan Profile}
\usage{
svyplan(...)

\method{update}{svyplan}(object, ...)
}
\arguments{
\item{...}{Named defaults to reuse across calls. Allowed names:
\code{alpha}, \code{N}, \code{deff}, \code{resp_rate},
\code{stage_cost}, \code{delta}, \code{rel_var}, \code{k}, \code{fixed_cost},
\code{unit_cost},
\code{alternative}, \code{method}, \code{ratio}, \code{overlap}, \code{rho},
\code{alloc}, \code{min_n}, \code{power_q}.}

\item{object}{A \code{svyplan} object to update.}
}
\value{
A \code{svyplan} object.
}
\description{
Create a reusable profile that captures shared design defaults
for survey sample size and power calculations. A plan can be passed
to functions like \code{\link[=n_prop]{n_prop()}}, \code{\link[=n_mean]{n_mean()}}, \code{\link[=power_prop]{power_prop()}}, or
\code{\link[=n_cluster]{n_cluster()}} either via the \code{plan} named argument or by piping:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{plan <- svyplan(deff = 1.8, resp_rate = 0.85, N = 50000)

# Named argument
n_prop(p = 0.3, moe = 0.05, plan = plan)

# Pipe
plan |> n_prop(0.3, moe = 0.05)
plan |> n_prop(p = 0.3, moe = 0.05)
plan |> n_cluster(cv = 0.05)
}\if{html}{\out{</div>}}

The profile stores design parameters that are shared across multiple
functions. Each default fills in only arguments not explicitly provided
by the caller. Explicit arguments always take precedence:

\if{html}{\out{<div class="sourceCode">}}\preformatted{plan <- svyplan(deff = 1.8, resp_rate = 0.85)
n_prop(p = 0.3, moe = 0.05, plan = plan)           # uses plan defaults
n_prop(p = 0.3, moe = 0.05, plan = plan, deff = 2)  # deff = 2 wins
}\if{html}{\out{</div>}}

Defaults are applied only when their names match the called function's
formals. Irrelevant defaults are silently ignored.

Estimand-specific values (\code{p}, \code{var}, \code{mu}, \code{moe}, \code{cv}, \code{n}, \code{power},
\code{effect}) should be passed directly to each function, not stored in
the plan.

When piping, the plan is detected automatically. The function's
original first argument (e.g., \code{p} in \code{\link[=n_prop]{n_prop()}}) can be passed
either positionally or by name.
}
\examples{
# Create a plan with common design parameters
plan <- svyplan(deff = 1.8, resp_rate = 0.85, N = 50000)

# Use via named argument
n_prop(p = 0.3, moe = 0.05, plan = plan)
n_mean(var = 100, mu = 50, cv = 0.05, plan = plan)
power_prop(p1 = 0.30, p2 = 0.35, plan = plan)

# Use via pipe
plan |> n_prop(0.3, moe = 0.05)
plan |> n_prop(p = 0.3, moe = 0.05)
plan |> n_mean(100, mu = 50, cv = 0.05)
plan |> power_prop(0.30, p2 = 0.35)

# Cluster context
cl_plan <- svyplan(stage_cost = c(500, 50), delta = 0.05, resp_rate = 0.85)
cl_plan |> n_cluster(cv = 0.05)

# Override a plan default
n_prop(p = 0.3, moe = 0.05, plan = plan, deff = 2.0)

}
\seealso{
\code{\link[=n_prop]{n_prop()}}, \code{\link[=n_mean]{n_mean()}}, \code{\link[=n_cluster]{n_cluster()}}, \code{\link[=power_prop]{power_prop()}},
\code{\link[=power_mean]{power_mean()}}, \code{\link[=power_did]{power_did()}}.
}
