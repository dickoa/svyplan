% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prec_multi.R
\name{prec_multi}
\alias{prec_multi}
\alias{prec_multi.default}
\alias{prec_multi.svyplan_n}
\alias{prec_multi.svyplan_cluster}
\title{Multi-Indicator Sampling Precision}
\usage{
prec_multi(targets, ...)

\method{prec_multi}{default}(targets, cost = NULL, budget = NULL, m = NULL, joint = FALSE, ...)

\method{prec_multi}{svyplan_n}(targets, ...)

\method{prec_multi}{svyplan_cluster}(targets, ...)
}
\arguments{
\item{targets}{For the default method: data frame with one row per
indicator (must contain an \code{n} column; see Details). For \code{svyplan_n}
or \code{svyplan_cluster} objects: a result from \code{\link[=n_multi]{n_multi()}}.}

\item{...}{Additional arguments passed to methods.}

\item{cost}{Numeric vector of per-stage costs. \code{NULL} (default) for
simple mode; length 2 or 3 for multistage mode.}

\item{budget}{Total budget (currently unused in precision mode).}

\item{m}{Fixed stage-1 sample size (currently unused in precision mode).}

\item{joint}{Logical (currently unused in precision mode).}
}
\value{
A \code{svyplan_prec} object with a \verb{$detail} data frame containing
per-indicator precision.
}
\description{
Compute the sampling error (SE, MOE, CV) for multiple survey indicators
given a sample size. This is the inverse of \code{\link[=n_multi]{n_multi()}}.
}
\details{
The \code{targets} data frame supports the following columns:

\describe{
\item{\code{name}}{Indicator label (optional).}
\item{\code{p}}{Expected proportion, in (0, 1). One of \code{p} or \code{var} per row.}
\item{\code{var}}{Population variance. One of \code{p} or \code{var} per row.}
\item{\code{mu}}{Population mean. Required for CV when \code{var} is specified.}
\item{\code{n}}{Sample size (required). For simple mode, a scalar per
indicator. For multistage, per-stage sizes can be provided as
\code{n}, \code{n2}, \code{n3} columns.}
\item{\code{alpha}}{Significance level (default 0.05).}
\item{\code{deff}}{Design effect multiplier (simple mode only, default 1).}
\item{\code{N}}{Population size (simple mode only, default Inf).}
\item{\code{resp_rate}}{Expected response rate (default 1).}
\item{\code{delta1}, \code{delta2}}{Homogeneity measures (multistage).}
\item{\code{rel_var}}{Unit relvariance. If omitted, derived from \code{p} or
\code{var}/\code{mu}.}
\item{\code{k1}, \code{k2}}{Ratio parameters (multistage, default 1).}
}

Any column not in the recognized set is treated as a \strong{domain variable}.
}
\examples{
# Simple mode: precision for three indicators at n = 400
targets <- data.frame(
  name = c("stunting", "vaccination", "anemia"),
  p    = c(0.30, 0.70, 0.10),
  n    = c(400, 400, 400)
)
prec_multi(targets)

}
\seealso{
\code{\link[=n_multi]{n_multi()}} for the inverse (compute n from precision targets),
\code{\link[=prec_prop]{prec_prop()}}, \code{\link[=prec_mean]{prec_mean()}} for single-indicator precision.
}
