% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_did.R
\name{power_did}
\alias{power_did}
\alias{power_did.default}
\title{Power Analysis for Difference-in-Differences Designs}
\usage{
power_did(treat, ...)

\method{power_did}{default}(
  treat,
  control,
  outcome = c("mean", "prop"),
  var = NULL,
  effect = NULL,
  n = NULL,
  power = 0.8,
  alpha = 0.05,
  N = Inf,
  deff = 1,
  resp_rate = 1,
  alternative = c("two.sided", "one.sided"),
  ratio = 1,
  overlap = 0,
  rho = 0,
  plan = NULL,
  ...
)
}
\arguments{
\item{treat}{Numeric length-2 vector for treated group outcomes:
\code{c(baseline, endline)}.}

\item{...}{Additional arguments passed to methods.}

\item{control}{Numeric length-2 vector for control group outcomes:
\code{c(baseline, endline)}.}

\item{outcome}{Outcome scale: \code{"mean"} (default) or \code{"prop"}.}

\item{var}{Outcome variance for \code{outcome = "mean"}.
Length 1: common variance for all four cells.
Length 2: group-specific variances \code{c(var_treat, var_control)},
assumed equal across waves.
Length 4: cell-specific variances in order
\code{c(var_treat_baseline, var_treat_endline, var_control_baseline, var_control_endline)}.}

\item{effect}{Absolute DiD effect size to detect (> 0).
Leave \code{NULL} to solve for MDE.}

\item{n}{Per-arm sample size per wave. Scalar (equal treated/control)
or length-2 vector \code{c(n_treat, n_control)}. Leave \code{NULL} to solve
for n.}

\item{power}{Target power, in (0, 1). Leave \code{NULL} to solve for power.}

\item{alpha}{Significance level, default 0.05.}

\item{N}{Population size for finite-population correction.
Scalar applies to both arms; length-2 vector \code{c(N_treat, N_control)}.
\code{Inf} (default) disables FPC.}

\item{deff}{Design effect multiplier (> 0).}

\item{resp_rate}{Expected response rate, in (0, 1]. Default 1.}

\item{alternative}{Character: \code{"two.sided"} (default) or \code{"one.sided"}.}

\item{ratio}{Allocation ratio \code{n_treat / n_control} (default 1).
Used only when solving for \code{n} (\code{n = NULL}).}

\item{overlap}{Panel overlap fraction in [0, 1] within each arm
across baseline and endline.}

\item{rho}{Correlation between baseline and endline outcomes within
overlapping units, in [0, 1].}

\item{plan}{Optional \code{\link[=svyplan]{svyplan()}} object providing design defaults.}
}
\value{
A \code{svyplan_power} object with components:
\describe{
\item{\code{n}}{Per-arm sample size (scalar or length-2).}
\item{\code{power}}{Achieved power.}
\item{\code{effect}}{DiD effect size.}
\item{\code{type}}{\code{"did_prop"} or \code{"did_mean"}.}
\item{\code{solved}}{Which quantity was solved for
(\code{"n"}, \code{"power"}, or \code{"mde"}).}
\item{\code{params}}{List of input parameters.}
}
}
\description{
Compute sample size, power, or minimum detectable effect (MDE) for a
two-group, two-period difference-in-differences (DiD) contrast.
Leave exactly one of \code{n}, \code{power}, or \code{effect} as \code{NULL}.
}
\details{
The DiD estimand is
\code{(treat_endline - treat_baseline) - (control_endline - control_baseline)}.

Per-arm variance of the before-after change accounts for panel overlap:

\deqn{V_{\text{arm}} = V_{\text{pre}} + V_{\text{post}}
  - 2 \cdot \text{overlap} \cdot \rho \cdot
  \sqrt{V_{\text{pre}} \cdot V_{\text{post}}}}

The DiD test statistic variance is then:

\deqn{V_d = \text{deff} \left(
  \frac{V_{\text{trt}} \cdot \text{fpc}_t}{n_t} +
  \frac{V_{\text{ctrl}} \cdot \text{fpc}_c}{n_c}
\right)}

When \code{overlap = 0}, this reduces to the classical flat-variance formula.
}
\examples{
# DiD sample size for means
power_did(
  treat = c(50, 55), control = c(50, 52),
  outcome = "mean", var = 100, effect = 3
)

# DiD power for proportions
power_did(
  treat = c(0.30, 0.36), control = c(0.30, 0.33),
  outcome = "prop", effect = 0.03, n = 800, power = NULL
)

# MDE for means with n = 500
power_did(
  treat = c(50, 55), control = c(50, 52),
  outcome = "mean", var = 100, effect = NULL, n = 500
)

# Panel overlap reduces required n
power_did(
  treat = c(0.50, 0.55), control = c(0.50, 0.48),
  outcome = "prop", effect = 0.07, overlap = 0.5, rho = 0.6
)

}
\references{
Valliant, R., Dever, J. A., & Kreuter, F. (2018). \emph{Practical Tools for
Designing and Weighting Survey Samples} (2nd ed.). Springer. Chapter 4.
}
\seealso{
\code{\link[=power_prop]{power_prop()}} for two-sample proportions, \code{\link[=power_mean]{power_mean()}} for
two-sample means.
}
