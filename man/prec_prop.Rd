% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prec_prop.R
\name{prec_prop}
\alias{prec_prop}
\alias{prec_prop.default}
\alias{prec_prop.svyplan_n}
\title{Sampling Precision for a Proportion}
\usage{
prec_prop(p, ...)

\method{prec_prop}{default}(
  p,
  n,
  alpha = 0.05,
  N = Inf,
  deff = 1,
  resp_rate = 1,
  method = "wald",
  ...
)

\method{prec_prop}{svyplan_n}(p, ...)
}
\arguments{
\item{p}{For the default method: expected proportion, in (0, 1).
For \code{svyplan_n} objects: a sample size result from \code{\link[=n_prop]{n_prop()}}.}

\item{...}{Additional arguments passed to methods.}

\item{n}{Sample size.}

\item{alpha}{Significance level, default 0.05.}

\item{N}{Population size. \code{Inf} (default) means no finite population
correction.}

\item{deff}{Design effect multiplier (> 0). Values < 1 are valid for
efficient designs (e.g., stratified sampling with Neyman allocation).}

\item{resp_rate}{Expected response rate, in (0, 1]. Default 1 (no
adjustment). The effective sample size is deflated by \code{resp_rate}.}

\item{method}{One of \code{"wald"} (default), \code{"wilson"}, or \code{"logodds"}.}
}
\value{
A \code{svyplan_prec} object with components \verb{$se}, \verb{$moe}, and \verb{$cv}.
}
\description{
Compute the sampling error (SE, margin of error, CV) for estimating a
population proportion given a sample size. This is the inverse of
\code{\link[=n_prop]{n_prop()}}.
}
\details{
Computes the standard error for the given sample size and design
parameters, then derives the margin of error and coefficient of
variation. The effective sample size is \code{n * resp_rate / deff}, with
optional finite population correction.

The Wald FPC uses the Cochran (1977, Ch. 3) form: the finite-population
correction for a Bernoulli proportion is \code{(N - n_eff) / (N - 1)}, not
the simpler \code{1 - n_eff / N} used for means.
}
\examples{
# Precision with n = 400
prec_prop(p = 0.3, n = 400)

# With design effect and response rate
prec_prop(p = 0.3, n = 400, deff = 1.5, resp_rate = 0.8)

}
\seealso{
\code{\link[=n_prop]{n_prop()}} for the inverse (compute n from a precision target),
\code{\link[=prec_mean]{prec_mean()}} for continuous variables.
}
