% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_alloc.R
\name{prec_alloc}
\alias{prec_alloc}
\alias{prec_alloc.default}
\alias{prec_alloc.svyplan_n}
\title{Precision for a Constrained Allocation}
\usage{
prec_alloc(x, ...)

\method{prec_alloc}{default}(
  x,
  n,
  alpha = 0.05,
  deff = 1,
  resp_rate = 1,
  unit_cost = NULL,
  plan = NULL,
  ...
)

\method{prec_alloc}{svyplan_n}(x, ...)
}
\arguments{
\item{x}{For the default method: a frame data frame with stratum metadata.
For \code{svyplan_n} objects: an allocation result from \code{\link[=n_alloc]{n_alloc()}}.}

\item{...}{Additional arguments passed to methods.}

\item{n}{Stratum sample sizes, length \code{nrow(x)} (default method only).}

\item{alpha}{Significance level, default 0.05.}

\item{deff}{Design effect multiplier (> 0).}

\item{resp_rate}{Expected response rate, in (0, 1]. Default 1.}

\item{unit_cost}{Optional scalar or length-\code{nrow(x)} vector of
per-stratum unit costs, overriding \code{x$cost_h}.}

\item{plan}{Optional \code{\link[=svyplan]{svyplan()}} object providing design defaults.}
}
\value{
A \code{svyplan_prec} object with \code{type = "alloc"}.
}
\description{
Compute aggregate precision for a stratum allocation.
}
\examples{
frame <- data.frame(
  N_h = c(4000, 3000, 3000),
  S_h = c(10, 15, 8),
  mean_h = c(50, 60, 55)
)
res <- n_alloc(frame, n = 600)
prec_alloc(res)

}
\seealso{
\code{\link[=n_alloc]{n_alloc()}}.
}
