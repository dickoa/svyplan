% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_alloc.R
\name{n_alloc}
\alias{n_alloc}
\alias{n_alloc.default}
\alias{n_alloc.svyplan_prec}
\title{Constrained Stratified Allocation}
\usage{
n_alloc(frame, ...)

\method{n_alloc}{default}(
  frame,
  n = NULL,
  cv = NULL,
  budget = NULL,
  alloc = c("neyman", "optimal", "proportional", "power"),
  unit_cost = NULL,
  alpha = 0.05,
  deff = 1,
  resp_rate = 1,
  min_n = NULL,
  power_q = 0.5,
  plan = NULL,
  ...
)

\method{n_alloc}{svyplan_prec}(frame, n = NULL, cv = NULL, budget = NULL, ...)
}
\arguments{
\item{frame}{For the default method: data frame with one row per stratum.
Must include \code{N_h} and either \code{S_h} or \code{var}.
Optional columns: \code{mean_h} (or \code{p_h}), \code{cost_h}, \code{max_weight}, \code{take_all}.
Any unrecognized columns are treated as domain identifiers.
For \code{svyplan_prec} objects: a precision result from \code{\link[=prec_alloc]{prec_alloc()}}.}

\item{...}{Additional arguments passed to methods.}

\item{n}{Total sample size. Specify exactly one of \code{n}, \code{cv}, or \code{budget}.}

\item{cv}{Target coefficient of variation (requires \code{mean_h} or \code{p_h}
in \code{frame}). When domain columns are present, this target is enforced
in each domain. Specify exactly one of \code{n}, \code{cv}, or \code{budget}.}

\item{budget}{Total field budget. Specify exactly one of \code{n}, \code{cv},
or \code{budget}.}

\item{alloc}{Allocation rule: \code{"neyman"} (default), \code{"optimal"},
\code{"proportional"}, or \code{"power"}.}

\item{unit_cost}{Optional scalar or length-\code{nrow(frame)} vector of
per-stratum unit costs, overriding \code{frame$cost_h}.}

\item{alpha}{Significance level, default 0.05.}

\item{deff}{Design effect multiplier (> 0).}

\item{resp_rate}{Expected response rate, in (0, 1]. Default 1.}

\item{min_n}{Optional minimum sample size per stratum.}

\item{power_q}{Bankier power parameter from 0 to 1, used when
\code{alloc = "power"}.}

\item{plan}{Optional \code{\link[=svyplan]{svyplan()}} object providing design defaults.}
}
\value{
A \code{svyplan_n} object with \code{type = "alloc"} and a stratum-level
allocation table in \verb{$detail}.
}
\description{
Compute a constrained stratum allocation under a fixed total sample size,
target CV, or budget.
}
\details{
Allocation is controlled by the \code{alloc} parameter (same methods as
\code{\link[=strata_bound]{strata_bound()}}):
\itemize{
\item \strong{proportional}: \eqn{n_h \propto N_h}
\item \strong{neyman}: \eqn{n_h \propto N_h S_h}
\item \strong{optimal}: \eqn{n_h \propto N_h S_h / \sqrt{c_h}}
\item \strong{power}: Bankier (1988), \eqn{n_h \propto S_h N_h^{power\_q}}
}

Stratum allocations are rounded to integers using the ORIC method
(Cont and Heidari, 2015). Constraints (\code{min_n}, \code{max_weight}, \code{take_all})
are enforced via recursive Neyman allocation (RNA, Wesolowski et al., 2021).

When \code{cv} is specified with domain columns, the algorithm finds the
minimum total \code{n} such that every domain achieves the target CV.

When \code{budget} is specified, the algorithm finds the maximum affordable
allocation under unit costs.
}
\examples{
frame <- data.frame(
  stratum = c("A", "B", "C"),
  N_h = c(4000, 3000, 3000),
  S_h = c(10, 15, 8),
  mean_h = c(50, 60, 55),
  cost_h = c(1, 1.5, 1)
)

n_alloc(frame, n = 600)
n_alloc(frame, cv = 0.03)

}
\references{
Valliant, R., Dever, J. A., & Kreuter, F. (2018). \emph{Practical Tools for
Designing and Weighting Survey Samples} (2nd ed.). Springer. Chapter 5.

Bankier, M. D. (1988). Power allocations: determining sample sizes for
subnational areas. \emph{The American Statistician}, 42(3), 174--177.
}
\seealso{
\code{\link[=prec_alloc]{prec_alloc()}}, \code{\link[=strata_bound]{strata_bound()}}.
}
