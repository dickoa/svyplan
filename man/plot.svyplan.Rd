% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.svyplan}
\alias{plot.svyplan}
\alias{plot.svyplan_strata}
\alias{plot.svyplan_power}
\title{Plot svyplan objects}
\usage{
\method{plot}{svyplan_strata}(x, ...)

\method{plot}{svyplan_power}(x, npoints = 101L, ...)
}
\arguments{
\item{x}{A svyplan object.}

\item{...}{Additional graphical parameters passed to \code{\link[=barplot]{barplot()}}
(for strata) or \code{\link[=plot]{plot()}} (for power). These override the defaults,
so you can set \code{main}, \code{col}, \code{ylab}, \code{xlab}, \code{ylim}, etc.}

\item{npoints}{Number of points in the power curve grid (default 101).}
}
\value{
\code{x}, invisibly.
}
\description{
Visualize sampling fractions per stratum or power curves from svyplan
results.
}
\details{
\code{plot.svyplan_strata()} draws a bar chart of per-stratum sampling
fractions (\code{f_h = n_h / N_h}) using \code{\link[=barplot]{barplot()}}. This shows how
intensively each stratum is sampled — under Neyman allocation,
high-variance strata get higher fractions. A dashed horizontal line
marks the overall sampling fraction (\code{n / N}). Defaults:
\code{col = "grey40"}, \code{ylab = "Sampling fraction (f_h)"}, \code{las = 2}.

\code{plot.svyplan_power()} draws the power-vs-sample-size curve using
\code{\link[=plot]{plot()}}. The solved point is shown as a filled dot, with dashed
reference lines at the computed power and sample size, and a dotted
line at the significance level. Defaults: \code{ylim = c(0, 1)},
\code{type = "l"}, \code{xlab = "Sample size (per group)"},
\code{ylab = "Power"}.
}
\examples{
# Sampling fraction per stratum
set.seed(1)
sb <- strata_bound(rlnorm(2000, 6, 1), n_strata = 4, n = 200,
                    method = "cumrootf")
plot(sb)

# Custom colour
plot(sb, col = "steelblue")

# Power curve with defaults
pw <- power_prop(p1 = 0.30, p2 = 0.40, power = 0.80)
plot(pw)

# Custom line width and colour
plot(pw, lwd = 2, col = "darkred")

}
