% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.svyplan}
\alias{predict.svyplan}
\alias{predict.svyplan_n}
\alias{predict.svyplan_cluster}
\alias{predict.svyplan_power}
\alias{predict.svyplan_prec}
\title{Grid Exploration for svyplan Objects}
\usage{
\method{predict}{svyplan_n}(object, newdata, ...)

\method{predict}{svyplan_cluster}(object, newdata, ...)

\method{predict}{svyplan_power}(object, newdata, ...)

\method{predict}{svyplan_prec}(object, newdata, ...)
}
\arguments{
\item{object}{A svyplan object (\code{svyplan_n}, \code{svyplan_cluster},
\code{svyplan_power}, or \code{svyplan_prec}).}

\item{newdata}{A data frame of parameter combinations to evaluate.
Column names must be valid parameters for the object type (see
Details). Parameters not in \code{newdata} stay at their original
values from the object.}

\item{...}{Ignored.}
}
\value{
A data frame with \code{newdata} columns followed by result
columns. The result columns depend on the object type:
\itemize{
\item \code{svyplan_n}: \code{n}, \code{se}, \code{moe}, \code{cv}
\item \code{svyplan_cluster}: \code{n1}, \code{n2}, (opt. \code{n3}), \code{total_n}, \code{cv}, \code{cost}
\item \code{svyplan_power}: \code{n}, \code{power}, \code{delta}
\item \code{svyplan_prec}: \code{se}, \code{moe}, \code{cv}
}
}
\description{
Evaluate a svyplan result at new parameter combinations.
Returns a data frame with the varied parameters and resulting
quantities, suitable for sensitivity analysis or plotting.
}
\details{
Valid parameters for \code{newdata} by object type:
\itemize{
\item \strong{\code{n_prop}}: \code{p}, \code{moe}, \code{cv}, \code{alpha}, \code{N}, \code{deff}, \code{resp_rate}
\item \strong{\code{n_mean}}: \code{var}, \code{mu}, \code{moe}, \code{cv}, \code{alpha}, \code{N}, \code{deff},
\code{resp_rate}
\item \strong{\code{n_cluster}}: \code{cv}, \code{budget}, \code{resp_rate}, \code{fixed_cost}
\item \strong{\code{power_prop}}: \code{p1}, \code{p2}, \code{n}, \code{power}, \code{alpha}, \code{N}, \code{deff},
\code{sides}, \code{overlap}, \code{rho}, \code{resp_rate} (excluding the solved-for
parameter)
\item \strong{\code{power_mean}}: \code{delta}, \code{var}, \code{n}, \code{power}, \code{alpha}, \code{N},
\code{deff}, \code{sides}, \code{overlap}, \code{rho}, \code{resp_rate} (excluding the
solved-for parameter)
\item \strong{\code{prec_prop}}: \code{p}, \code{n}, \code{alpha}, \code{N}, \code{deff}, \code{resp_rate}
\item \strong{\code{prec_mean}}: \code{var}, \code{n}, \code{mu}, \code{alpha}, \code{N}, \code{deff}, \code{resp_rate}
}

For \code{svyplan_n} objects, \code{moe} and \code{cv} are mutually exclusive in
\code{newdata}. If one appears, that mode is used; if neither, the
original mode is preserved.

Similarly, for \code{svyplan_cluster} objects, \code{cv} and \code{budget} are
mutually exclusive.

Multi-indicator (\code{n_multi}) and multi-indicator cluster results are
not supported; use the underlying single-indicator functions instead.

If evaluation fails for a particular row (e.g. invalid parameter
combinations), that row's result columns are \code{NA} and a warning is
issued.
}
\examples{
# Sensitivity of sample size to deff and response rate
x <- n_prop(p = 0.3, moe = 0.05, deff = 1.5)
predict(x, expand.grid(
  deff = seq(1, 3, 0.5),
  resp_rate = c(0.7, 0.8, 0.9)
))

# Power curve: how does power vary with sample size?
pw <- power_prop(p1 = 0.30, p2 = 0.35, n = 500, power = NULL)
predict(pw, data.frame(n = seq(100, 1000, 100)))

}
