% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_prop.R
\name{power_prop}
\alias{power_prop}
\alias{power_prop.default}
\title{Power Analysis for Proportions}
\usage{
power_prop(p1, ...)

\method{power_prop}{default}(
  p1,
  ...,
  p2 = NULL,
  n = NULL,
  power = 0.8,
  alpha = 0.05,
  N = Inf,
  deff = 1,
  resp_rate = 1,
  alternative = c("two.sided", "one.sided"),
  ratio = 1,
  overlap = 0,
  rho = 0,
  method = c("wald", "arcsine", "logodds"),
  plan = NULL
)
}
\arguments{
\item{p1}{Baseline proportion, in (0, 1).}

\item{...}{Additional arguments passed to methods. Unused arguments are rejected.}

\item{p2}{Alternative proportion, in (0, 1). Leave \code{NULL} to solve for
the minimum detectable effect (MDE). The solver searches both above and
below \code{p1} and returns the alternative closest to \code{p1} that achieves the
target power. When \code{p1} is near 0 or 1, the MDE may only be detectable
in one direction.}

\item{n}{Per-group sample size, measured as gross units drawn and bounded
by the corresponding finite \code{N}. Scalar (equal groups) or length-2 vector
\code{c(n1, n2)} for unequal groups. Leave \code{NULL} to solve for sample size.}

\item{power}{Target power, in (0, 1). Leave \code{NULL} to solve for power.}

\item{alpha}{Significance level, default 0.05.}

\item{N}{Population size for finite-population correction. A scalar applies
to both groups. A length-2 vector \code{c(N1, N2)} sets group-specific
population sizes. \code{Inf} disables FPC for the corresponding group.}

\item{deff}{Design effect multiplier (> 0). Values < 1 are valid for
efficient designs (e.g., stratified sampling with Neyman allocation).}

\item{resp_rate}{Expected response rate, in (0, 1]. Default 1 (no
adjustment). The required sample size is inflated by \code{1 / resp_rate}.}

\item{alternative}{Character: \code{"two.sided"} (default) or \code{"one.sided"}.
Use \code{"two.sided"} when either an increase or decrease matters (the
usual default). Use \code{"one.sided"} when only one direction is of
interest (e.g. "has the intervention \emph{reduced} stunting?"), which
requires a smaller sample for the same power.}

\item{ratio}{Allocation ratio n1/n2 (default 1). Only used when solving
for n (\code{n = NULL}). For example, \code{ratio = 2} means group 1 gets twice
the sample of group 2.}

\item{overlap}{Panel overlap fraction in [0, 1], for repeated surveys.
Defined as the fraction of group 1 that also appears in group 2
(\code{overlap = n12 / n1}). Only supported with \code{method = "wald"}.}

\item{rho}{Correlation between occasions in [0, 1].}

\item{method}{Variance method: \code{"wald"} (default), \code{"arcsine"}, or
\code{"logodds"}. Arcsine and log-odds transforms are variance-stabilizing
and perform better for rare or extreme proportions (Valliant,
2018, sections 4.3.4--4.3.5).}

\item{plan}{Optional \code{\link[=svyplan]{svyplan()}} object providing design defaults.}
}
\value{
A \code{svyplan_power} object with components:
\describe{
\item{\code{n}}{Per-group sample size (scalar or length-2 for unequal groups).}
\item{\code{power}}{Achieved power.}
\item{\code{effect}}{Difference in proportions (\code{abs(p2 - p1)}).}
\item{\code{solved}}{Which quantity was solved for
(\code{"n"}, \code{"power"}, or \code{"mde"}).}
\item{\code{params}}{List of input parameters.}
}
}
\description{
Compute sample size, power, or minimum detectable effect (MDE) for a
two-sample test of proportions. Leave exactly one of \code{n}, \code{power}, or
\code{p2} as \code{NULL} to solve for that quantity.
}
\details{
\subsection{Choosing a method}{

\describe{
\item{\code{"wald"} (default)}{Standard Wald test. Appropriate for
proportions in the 0.2--0.8 range. The only method that
supports panel \code{overlap}.}
\item{\code{"arcsine"}}{Arcsine-transformed test. The arcsine variance
\eqn{1/(4n)} is approximately constant in \eqn{p}, making it
more accurate when either proportion is below 0.15 or above 0.85.}
\item{\code{"logodds"}}{Log-odds (logit) test with separate null and
alternative variances. Also suitable for rare or extreme
proportions. Uses Valliant Eq (4.23)/(4.24).}
}

For proportions in the 0.2--0.8 range, all three methods give similar
results. For rare or extreme proportions, \code{"arcsine"} or \code{"logodds"}
are more reliable.
}
}
\examples{
# Sample size to detect a 5pp change from 30\%
power_prop(p1 = 0.30, p2 = 0.35)

# Power given n = 500
power_prop(p1 = 0.30, p2 = 0.35, n = 500, power = NULL)

# MDE with n = 1000
power_prop(p1 = 0.30, n = 1000)

# Arcsine transform for rare proportions
power_prop(p1 = 0.15, p2 = 0.18, alternative = "one.sided",
           method = "arcsine")

# Log-odds transform
power_prop(p1 = 0.15, p2 = 0.18, alternative = "one.sided",
           method = "logodds")

# Allocation ratio 2:1
power_prop(p1 = 0.30, p2 = 0.35, ratio = 2)

}
\references{
Valliant, R., Dever, J. A., & Kreuter, F. (2018). \emph{Practical Tools for
Designing and Weighting Survey Samples} (2nd ed.). Springer. Chapter 4.

Cochran, W. G. (1977). \emph{Sampling Techniques} (3rd ed.). Wiley.
}
\seealso{
\code{\link[=power_mean]{power_mean()}} for continuous outcomes, \code{\link[=power_did]{power_did()}} for
difference-in-differences, \code{\link[=n_prop]{n_prop()}} for estimation precision.
}
